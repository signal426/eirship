use daemonize::Daemonize;
use std::fs::File;

pub struct Eirship {
    user: String,
    group: String,
    stdout: String,
    stderr: String,
}

impl Eirship {
    pub fn new(()) -> Self {


    }
}

